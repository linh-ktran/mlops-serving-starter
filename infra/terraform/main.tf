locals {
  endpoint_name        = "${var.name_prefix}-endpoint"
  endpoint_config_name = "${var.name_prefix}-endpoint-config"
  model_name           = "${var.name_prefix}-model"
  schedule_name        = "${var.name_prefix}-pipeline-schedule"
}

